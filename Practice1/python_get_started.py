print("write exit() or Ctrl-Z to exit")
# By just writing the word python (or py), we could know our version of python 

import sys
print(sys.version)
# By this we also could check our version of python